<div class="flex flex-col items-center justify-center p-7">
    <div
        class="max-w-3xl w-full bg-white rounded-xl shadow-2xl overflow-hidden transition-all duration-500 hover:shadow-lg">
        <div class="bg-primary p-6 text-center">
            <div class="shake-animation inline-block">
                <i data-feather="x-octagon" class="w-16 h-16 text-white stroke-1"></i>
            </div>
        </div>

        <div class="p-8 text-center">
            <h1 class="text-3xl font-bold text-dark mb-2">User Cancelled</h1>
            <p class="text-gray-600 mb-6">This transaction was cancelled.No charges were sent</p>

            <div class="space-y-4">
                <button
                    class="hidden w-full bg-secondary hover:bg-yellow-500 text-dark font-bold py-3 px-4 rounded-lg transition duration-300  items-center justify-center">
                    <i data-feather="rotate-ccw" class="mr-2"></i>
                    Try Again
                </button>
                <a asp-action="Index" asp-controller="Home"
                    class="w-full bg-accent hover:bg-orange-600 text-white font-bold py-3 px-4 rounded-lg transition duration-300 flex items-center justify-center">
                    <i data-feather="home" class="mr-2"></i>
                    Back to Home
                </a>
            </div>
        </div>

        <div class="bg-gray-100 p-4 text-center border-t border-gray-200">
            <p class="text-sm text-gray-600">Need help? <a href="#" class="text-primary hover:underline">Contact
                    support</a></p>
        </div>
    </div>
</div>
<script>
    feather.replace();
    document.querySelector('.shake-animation').addEventListener('animationend', function () {
        this.classList.remove('shake-animation');
    });
</script>